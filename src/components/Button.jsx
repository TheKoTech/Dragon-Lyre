import React from 'react';

const Button = () => {
	/**
	 * Creates a directory with given name.
	 * @param {string} name
	 */
	function _createDirectory(name) {

	}

	return (
		<button style={{width: 200, height: 50, fontSize: 20}}>
			Play sound
		</button>
	);
};

export default Button;